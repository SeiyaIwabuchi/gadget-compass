// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://seiyaiwabuchi.github.io',
    base: '/gadget-compass/gadget-compass/dist',
});
